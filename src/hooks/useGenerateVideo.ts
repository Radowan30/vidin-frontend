/**
 * Re-export from context for backwards compatibility.
 * 
 * @deprecated Use `useVideoGeneration` from `../context/VideoGenerationContext` instead.
 */
export { useVideoGeneration as useGenerateVideo } from '../context/VideoGenerationContext'
